defaults:
  - baseline
  - _self_

# Teensy config for quick development iterations
# Inherits all settings from baseline.yaml, overriding only what differs

project:
  experiment_name: "teensy"
  video_path: "./data/incoming/movies/teensy.mov"

opensplat:
  num_iters: 50

validation:
  image: "frame_0001.jpg"
